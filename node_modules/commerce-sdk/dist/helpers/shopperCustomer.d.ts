import { ShopperToken } from "@commerce-apps/core";
import * as sdk from "../";
declare type AuthFuncParamType = Parameters<sdk.Customer.ShopperCustomers["authorizeCustomer"]>[0];
declare type CustomerType = sdk.Customer.ShopperCustomers.Customer;
/**
 * This wraps the parameters for the authorization call to retrieve a token.
 * That call can be made directly, this is here just for convenience
 * ```ts
 * const clientConfig: ClientConfig = {
 *  parameters: {
 *      clientId: "XXXXXX",
 *      organizationId: "XXXX",
 *      shortCode: "XXX",
 *      siteId: "XX"
 *  }
 * };
 * helpers.getShopperToken(clientConfig, { type: "guest" })
 * ```
 * @param clientConfig - Client configuration properties
 * @param body - Post body required for authorization
 * @returns the retrieved Shopper token
 */
export declare function getShopperToken(clientConfig: sdk.ClientConfig, body: AuthFuncParamType["body"]): Promise<ShopperToken<CustomerType>>;
export {};
